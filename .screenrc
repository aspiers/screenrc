# -*- mode: sh -*-

source $ZDOTDIR/.screen.d/COMMON

# Default startup windows (-t <title>).
# Presumably if I'm using screen I want at least a few windows.
screen
screen
screen
screen
# need pam_xauth
# su - breaks xauth
screen su
screen su
#screen sudo zsh
#screen sudo zsh
# start with 0th window
select 0

