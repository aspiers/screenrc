# Only as a fall-back for when titles are not set "manually"
# via cx, or tmux's rename-window command.
#
# At one point there was a bug where automatic renaming
# would stomp over manual names, as described in the comments of:
#
# http://blog.hawkhost.com/2010/06/28/tmux-the-terminal-multiplexer/
#
# but it looks like it was fixed by r2775 (OpenBSD patchset 1093)
set -g automatic-rename on

# See ~/org/notes/Terminals.org
#set -g default-terminal screen-256color-bce-it
set -g default-terminal screen-256color

set -g history-limit 20000
set -g set-clipboard on
set -g set-titles on
set -g wrap-search off

#update-environment "DISPLAY SSH_ASKPASS SSH_AUTH_SOCK SSH_AGENT_PID SSH_CONNECTION WINDOWID XAUTHORITY XAUTHLOCALHOSTNAME"
